import { Notes } from 'mdx-deck';
import main from '@mdx-deck/themes/future';
import highlight from '@mdx-deck/themes/syntax-highlighter-prism';

export const themes = [main, highlight];

File structure
 - typescript generation
 - adding resolver
 - adding model
 - services
 - Firebase.ts

Writing code
 - db/getVal
 - auth
 - eslint

Tests
 - generating types
 - non null assertion / any
 - requests files

Running
 - dev/prod
 - spy

CLI

scratch

